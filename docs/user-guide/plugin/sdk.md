---
title: SDK
---

The Ziggy SDK provides a bridge between your Ziggy instance, the CLI and Custom Block Plugins.

## Installation
When using the CLI, this is installed automatically, so it is not necessary to explicitly install it.

Install the SDK from 

```shell
npm install -g github:fmay/ziggy-sdk.git
```
